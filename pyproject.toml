[project]
name = "d2ql"
version = "0.1.0"
description = "Scheudler development and simulation enviornment based on cloudsim"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "py4j>=0.10.9.9",
    "torch>=2.13.0",
    "pyyaml>=6.0.1",
    "gymnasium>=0.29.1"
]

[project.scripts]
build-deps = "scripts.build_deps:main"

